<Grid x:Class="OC.Assistant.PluginSystem.Plugin"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            mc:Ignorable="d" 
            d:DesignHeight="36" d:DesignWidth="800" x:ClassModifier="internal">
    <Button x:Name="EditButton" 
            Style="{DynamicResource SelectButton}" 
            HorizontalAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            Click="EditButton_Click">
        <StackPanel Orientation="Horizontal">
            <Border Margin="2 0" CornerRadius="{DynamicResource ControlCornerRadius}" Background="{DynamicResource Accent2Brush}">
                <TextBlock Margin="5 0" x:Name="NameText"/>
            </Border>
            <Border Margin="2 0" CornerRadius="{DynamicResource ControlCornerRadius}" Background="{DynamicResource White5Brush}">
                <TextBlock Margin="5 0" x:Name="TypeText"/>
            </Border>
            <Border Margin="2 0" CornerRadius="{DynamicResource ControlCornerRadius}" Background="{DynamicResource White5Brush}">
                <TextBlock Margin="5 0" x:Name="ClientTypeText"/>
            </Border>
        </StackPanel>
    </Button>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="StartStopButton" Click="StartStopButton_Click"
                    Width="36" 
                    Margin="4"
                    Padding="0" 
                    ToolTip="Start/Stop" 
                    VerticalAlignment="Stretch">
                <Grid>
                    <ContentControl
                        x:Name="StartIcon"
                        Foreground="{DynamicResource SuccessBrush}"
                        Template="{DynamicResource IconTemplate}"
                        Content="{DynamicResource IconPlay}"/>
                    <ContentControl
                        x:Name="StopIcon" 
                        Foreground="{DynamicResource TransparentBrush}"
                        Template="{DynamicResource IconTemplate}"
                        Content="{DynamicResource IconStop}"/>
                </Grid>
            </Button>
            <Button Click="RemoveButton_Click" 
                    Width="36" 
                    Margin="4" 
                    Padding="0" 
                    ToolTip="Remove" 
                    VerticalAlignment="Stretch">
                <ContentControl
                    Template="{DynamicResource IconTemplate}"
                    Content="{DynamicResource IconDelete}"/>
            </Button>
        </StackPanel>
</Grid>